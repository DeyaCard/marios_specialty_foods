<%= render "layouts/errors", :object => @product %>

<%= form_for @product do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :cost %>
  <%= f.text_field :cost %>
  <br>
  <%= f.label :country_of_origin %>
  <%= f.select(:country_of_origin, Product::COUNTRY_LIST) %> 
  <br>  
  <%= f.submit %>
<% end %>